# 작업내용 01 — 2026-04-12 세션

## 1. DSM 자동계산 코드 정리

### 문제
이전 대화에서 `design_member`에 `_try_auto_dsm` 호출을 추가했으나, 함수 본체가 API 오류로 미생성 → NameError 발생 위험.

### 조치
- `aisi_s100.py:145-149`의 `_try_auto_dsm` 호출 코드 삭제
- FSM 해석에서 Mcrl/Mcrd가 정확히 전달되고 있으므로 auto-DSM 불필요

---

## 2. Cold Work (§A3.3.2) Fya 적용 로직 수정

### 문제 (aisi_s100.py:575-585)
기존: "좌굴 감소가 있으면 Fya 무조건 거부" — 과도하게 보수적  
AISI S100-16 §A3.3.2는 Fya를 Fy 대신 모든 설계 조항에 대입 허용 (좌굴 여부 무관)

### 수정
- Fya로 실제 재계산 수행 → Mn_cw vs Mn_virgin 비교
- Fya 적용 시 강도 개선이 없으면 virgin Fy 복원 + 구체적 사유 경고

---

## 3. Cold Work C값 계산 오류 수정

### 문제 (_estimate_cold_work_areas)
- 분자: 전체 단면 코너 **4개** → 제어 플랜지 코너 **2개**여야 함
- 분모: flat 폭만 (코너 미포함) → flat + 코너 arc **전체** 포함해야 함
- C가 약 2배 과대 → Fya 과대 증가

### 수정 (aisi_s100.py:101-147)
- 휨부재: 제어 플랜지 기준으로 코너 수 결정 (C/Z=2, Hat=2, Track=1)
- 분모에 코너 arc 면적 포함
- 예시: C-200×50×15×1.5 → C: 0.48→0.17 (정상)

---

## 4. Lap Splice 접합부 — 용량 검증 추가

### 문제 (lap_connection.py)
"필요 패스너 수를 무한히 늘려주기만" 하는 산정 도구 → 항상 OK

### 수정
- Step 5: 배치 가능 최대 패스너 수 (Lap 길이 제약)
- Step 7: Demand vs Capacity 검증 — DCR = V_transfer / (n_actual × Pns)
- `pass`, `utilization` 필드 추가
- UI: 종합 판정 배너 (OK/NG + DCR)

---

## 5. Lap Splice — 휨강도 검토 추가 (Step 8)

### 문제
패스너 설계만 수행, AISI Example II-2A에서 요구하는 Mu ≤ φMn 검증 없음

### 수정 (lap_connection.py)
- Step 8: Lap 구간 휨강도 검토 (§F3, 2겹 부재 합산)
- Mn_lap = 2 × Se × Fy, DCR = Mu / φMn_lap
- Se를 UI에서 단면 속성으로 전달
- `overall_pass`에 `flexure_ok` 포함

---

## 6. Lap Splice — Mu 단위 오류 수정

### 문제
- `locations.Mu` = kip-ft, `conn-Mu` input = kip-in (moment 단위)
- `_autoFillLapInputs`에서 변환 없이 `toDisplay(값, 'moment')` 호출 → 12배 차이

### 수정 (app.js:3881)
`toDisplay(Math.abs(maxMu) * 12, 'moment')` — kip-ft → kip-in 변환 후 표시

---

## 7. 설계 Mu — Lap 끝단 부모멘트 사용

### 문제
모든 locations에서 최대 |Mu| 선택 → 지점 최대 부모멘트(2겹 단면)가 단일 단면 검토에 사용

### 수정
- **StcfsdPanel.ts:1392-1402**: `negMuSupport` (지점 최대, Lap 2겹용) vs `negMuLapEnd` (Lap끝, 단일 단면용) 분리
- **app.js:3787-3813**: `design-Mx` auto-fill에서 Lap이 있으면 Lap end 부모멘트 사용
- 6b(단일 단면): negMuLapEnd, 6c(Lap 2겹): negMuSupport

---

## 8. Lap 없는 지점에 Lap end 표시 방지

### 문제
`extract_critical_locations`가 단일 `laps` dict만 사용 → 모든 내부 지점에 Lap end 추가

### 수정
- `beam_analysis.py:500-550`: `laps_per_support` 지점별 배열 참조
- `required_strength.py`: `laps_with_detail['_per_support']`로 지점별 정보 전달

---

## 9. 모멘트도 높이/겹침 해소

### 수정 (app.js:3409)
- 기존 H=110/140 → H=130/150/170, PAD_T=18→22, PAD_B 증가

---

## 10. 모멘트도/전단력도 — Lap 끝 위치 마커 표시

### 수정
- `renderDiagramSVG`에 `annotations` 파라미터 추가
- Lap end 위치에 보라색 점선 + 마커 + 값 라벨 표시
- 보고서용 다이어그램에도 동일 적용

---

## 11. 접합부 탭 UI 개선

### Lap Splice
- 기본 열림 상태 (data-expanded="true")
- 하중분석 후 좌우 Lap길이, Mu, Vu 자동 채우기
- 각 입력 필드에 설명 추가

### 단일 접합부
- 제목: "단일 접합부 설계" → "단일 접합부 강도 계산"
- 설명 문구 추가, 각 입력 필드에 힌트

### PAF Fuf 입력 추가
- `conn-paf-row`에 Fuf 입력 (기본 414 MPa = 60 ksi)
- PAF 선택 시 표시/숨김, 데이터 전달

---

## 12. 접합부 SVG 다이어그램 전면 개선

### Lap SVG (`_drawLapSvg`)
- C단면 측면도 (웹+플랜지+립)
- 패스너 X표시, 간격(s)/edge distance(e)/최소 Lap(1.5d) 치수
- OK/NG 색상, V/Capacity/DCR 하단 표시, Lap zone 음영

### 단일 접합부 SVG (`_drawConnectionSvg`)
- 판재 해칭, Screw 머리/Bolt 육각/PAF 삼각 표현
- Pu 방향 화살표, AWS 용접 심볼, CJP/PJP 구분
- Rn/φRn/DCR/지배 파괴모드 하단 표시

---

## 13. JS 렌더링 키 불일치 수정

### 문제
`renderConnectionResult`에서 `ls.mode` 참조 → Python은 `ls.name` 반환

### 수정 (app.js:867-871)
`ls.mode` → `ls.name`, `ls.phi_Rn` → `ls.design_strength`

---

## 14. AISI Chapter J 접합 공식 검증

### 결과
| 항목 | 판정 |
|------|------|
| Screw Bearing (§J4.3.1) — Fu 사용 | 정확 |
| Arc Seam Tear (§J2.3.2.1) — 2.5 계수 | 정확 |
| Fillet Weld (§J2.5) — 0.75 계수 | 정확 |
| Bolt Bearing (§J3.3.1) | 정확 |
| Edge Distance (§J3.2, §J4.2) — 1.5d | 정확 |
| Safety Factors | 모두 정확 |

---

## 15. DSM 공식 검증 (§F3.2.1, §F4.1)

### 결과
- Mnl = [1-0.15(Mcrl/Mne)^0.4](Mcrl/Mne)^0.4 × Mne — **정확**
- Mnd = [1-0.22(Mcrd/My)^0.5](Mcrd/My)^0.5 × My — **정확**
- λl 한계 0.776, λd 한계 0.673 — **정확**
- Fn 계산 (§F2.1-3~5) — **정확**
- Fcre C/Z-section (§F2.1.1, §F2.1.3) — **정확**
- 값이 비슷한 것은 단면이 좌굴에 강할 때의 정상 동작

---

## 16. 정/부모멘트 구간 별도 Cb/Lb 설계

### 문제
- 정모멘트 구간: 데크 유무와 무관하게 항상 Lb=0 하드코딩
- 데크 없으면 정모멘트 구간도 Lb/Cb 계산 필요

### 수정
#### UI (StcfsdPanel.ts)
- 기존 단일 Cb/Lb → 정모멘트(+M)/부모멘트(-M) 구간 분리
- Lb(+), Cb(+), Lb(-), Cb(-) 입력 칸

#### Auto-fill (app.js)
- 정모멘트: positive_region.Ly_in/Cb
- 부모멘트: negative_region.Ly_in/Cb

#### Python (aisi_s100.py)
- `_design_flexure` 결과에 `positive_region` 추가
- Lb_pos, Cb_pos로 별도 Fcre/Mne/Mnl/Mnd/Mn 계산

#### 결과 표시 (app.js)
- 부모멘트 구간 라벨 + 기존 강도 카드
- 정모멘트 구간 결과 박스 (Fcre, Mne, Mnl, Mnd, Mn, φMn)

---

## 17. 부모멘트 영역 계산 — 데크 무관 실행

### 문제
`bracing.py`: 부모멘트 계산이 `if deck_type in (...)` 블록 안에 있어서 데크 없으면 미실행  
→ `negative_regions = []` → fallback: Lb = 전체 스팬 길이

### 수정
- 부모멘트 영역 계산(변곡점~Lap끝)을 데크 조건 밖으로 이동
- `negative_region_gov`에 `start_ft`, `end_ft`, `Cb_detail`, `M1`, `M2` 포함
- Fallback에도 `start_ft`/`end_ft` + `negative_regions` 배열 포함
- 모멘트도 치수선 복원

---

## 수정 파일 목록

| 파일 | 주요 변경 |
|------|-----------|
| `python/design/aisi_s100.py` | auto-DSM 제거, Cold Work 로직, C값 계산, 정모멘트 구간 결과 |
| `python/design/dsm_strength.py` | (변경 없음 — 공식 검증 완료) |
| `python/design/special_topics.py` | (변경 없음 — 공식 정확) |
| `python/design/global_buckling.py` | (변경 없음 — 공식 정확) |
| `python/design/lap_connection.py` | 용량 검증, 휨강도 검토, edge distance |
| `python/design/connections.py` | (변경 없음 — 공식 정확) |
| `python/design/loads/beam_analysis.py` | Lap end 지점별 필터, extract_critical_locations |
| `python/design/loads/bracing.py` | 부모멘트 계산 데크 무관 실행, 정모멘트 비지지 계산 |
| `python/design/loads/required_strength.py` | laps_with_detail, 정모멘트 region, fallback 보완 |
| `src/webview/StcfsdPanel.ts` | Lap Splice 열림, Cb/Lb 분리, PAF Fuf, 설명 추가 |
| `webview/js/app.js` | SVG 개선, 키 수정, 단위 수정, auto-fill, 결과 표시 |
