# 작업내용 02 — 2026-07-04~05 세션

xhigh 코드 리뷰(설계 엔진) + GUI 인터페이스 전면 점검·개선 세션.

---

# Part A. 설계 엔진 결함 수정 (xhigh 리뷰 6건)

워크플로 기반 다중 에이전트 리뷰로 발견·검증된 결함 전부 수정. 63/63 테스트 통과.

## A1. §C1.2.1.1 약축 P-δ 증폭 미작동 (비안전측) — `aisi_s100.py`

### 문제
`_design_combined()`가 `props['ry']`를 읽지만 자동생성 props 경로(`_auto_generate_props` → `grosprop()`)는
약축 회전반경을 `rz` 키로만 반환 → `ry`가 항상 0 → alpha_y가 항상 1.0으로 고정.
축력+약축휨 조합에서 Muy 증폭이 조용히 생략되어 **비안전측** 판정 가능.

### 수정
- `_auto_generate_props()`: `props.setdefault('ry', props.get('rz', 0))` 별칭 추가
- `_design_combined()`: `ry = props.get('ry', 0) or props.get('rz', 0)` fallback

## A2. block_shear() ASD/LRFD 분기·Table J6-1 계수 혼용 — `special_topics.py`

### 문제
1. 가용강도 선택이 `design_method`가 아니라 `phi`의 truthy 여부로 분기 —
   `omega_factor`만 넘긴 ASD 의도 호출이 LRFD(φRn)로 계산됨
2. 기본 φ=0.65는 볼트값, 기본 Ω=2.50은 용접값 — 설계법에 따라 서로 다른 체결재를 가정.
   용접 LRFD 강도 약 8% 과대(비안전측), 볼트 ASD 약 11% 과소
3. MCP 툴 `check_block_shear`에 `design_method` 파라미터 자체가 없어 ASD 결과 산출 불가

### 수정
- `connection_type` 파라미터 신설 (bolted: φ=0.65/Ω=2.22, welded: φ=0.60/Ω=2.50)
- `design_method` 문자열로 ASD/LRFD 분기, 결과에 사용된 φ/Ω/설계법 명시
- `src/mcp/server.ts` `check_block_shear` 스키마에 `design_method`/`connection_type` 추가

## A3. 그루브 용접 §J2.1(b) 지배식 판정 — `connections.py`

### 문제
Eq. J2.1-2(용접금속, φ0.80/Ω1.90) vs J2.1-3(모재, φ0.90/Ω1.70) 지배식 선택이
design_method와 무관하게 항상 ASD 비율(`Rn/1.90 ≤ Rn/1.70`)로 판정 —
두 설계법의 교차점이 달라 LRFD에서 잘못된 지배식 선택 가능.

### 수정
LRFD는 `0.80·Rn₂ ≤ 0.90·Rn₃`, ASD는 기존 비율로 분기.

## A4. compute_beam_Fcre() 계산서 상세 잔존 — `global_buckling.py`

### 문제
조기 반환 분기(물성 부족, Ky·Ly≤0 등)가 `_last_detail` 함수 속성을 갱신하지 않아
장수명 서버 프로세스에서 **이전 호출의 수식 상세**가 다음 계산서에 그대로 표시됨.

### 수정
함수 진입 시 `compute_beam_Fcre._last_detail = {}` 초기화.

## A5. _design_flexure Mn≤0 가드 부재 — `aisi_s100.py`

### 문제
`_design_compression`에는 "Pn≤0이면 무조건 pass=False" 가드가 있으나 휨에는 없어,
단면물성 실패로 Mn=0이어도 Mu 미입력 시 `pass=None`("미검토")으로 표시 — 0강도 실패 은폐.

### 수정
압축과 동일한 가드 추가: `Mn <= 0 → pass_flag = False`.

---

# Part B. GUI 인터페이스 개선 (전면 점검)

StcfsdPanel.ts(2839줄)/app.js(6537줄)/designState.js 전수 분석(병렬 에이전트 2기) 후
중복 인터페이스·혼란 UX·중복 절차·결함을 일괄 수정.

## B1. 기능적 죽은 UI → 실동작 (결함 수정)

| 항목 | 문제 | 수정 |
|------|------|------|
| 절점/요소 표 편집 | 편집 가능해 보이지만 model에 미반영 (입력이 조용히 무시됨) | 테이블 위임 리스너로 편집 → model 반영 → SVG/성질 갱신 → 확장측 `updateModel`(해석 무효화 포함) |
| 모드 분류 (G/D/L/O) | 실행 버튼이 없어 MCP 경유로만 채워지는 고아 캔버스 | 후처리 탭에 "모드 분류 실행" 버튼 추가, `_classifyModes`가 확장측 모델·해석결과로 payload 구성하도록 수정 |
| `_drawLapSvg` 립 깊이 | 존재하지 않는 `tpl-C` 참조 → 립이 항상 추정값 | `tpl-D`로 수정 + 인라인 `/25.4` 대신 `fromDisplay()` 사용 |
| 트리뷰·보고서 강종 | 존재하지 않는 `select-steel-grade` 참조 → 강종 항상 공란/'Custom' | `input-steel-grade`로 수정 + selectedIndex 가드 |

## B2. 잠재 결함 수정

| 항목 | 문제 | 수정 |
|------|------|------|
| 리스너 누적 | `populatePostSelects`가 해석마다 `addEventListener` 중복 부착 → N회 해석 후 모드형상 N번 재렌더 | `onchange` 할당으로 교체 |
| dsmResult 도착 순서 race | `analysisComplete`보다 늦게 오면 기본 반파장이 index 0으로 고정 | dsmResult 수신 시 `populatePostSelects()` 재실행 + 모드형상 재렌더 |
| buildSpanTable 스코프 | if-블록 내 함수선언을 외부(`restoreAllDesignInputs`)에서 참조 — sloppy-mode 호이스팅 의존 | 최상위 스코프로 이동, 내부 가드 추가 |
| CSP 차단 폰트 | theme.css의 구글 폰트 `@import`가 CSP(`font-src`)에 항상 차단 — 죽은 네트워크 요청 | 제거, VS Code 폰트 변수 사용 |
| 죽은 코드 | `_dimLine`, `_niceStep`(미사용), design-fy 역방향 동기화(readonly라 발화 불가), 레거시 `select-steel-grade` 핸들러, designState의 no-op 저장/복원 | 제거 |
| 자동입력 미검증 | 하중분석 자동입력(`setValue`)이 input 이벤트를 발화하지 않아 검증/미리보기 핸들러 미반응 | `setValueAndNotify()` 헬퍼 신설, 자동입력 경로에 적용 |

## B3. 중복 인터페이스·절차 정리

| 항목 | 문제 | 수정 |
|------|------|------|
| Fy/Fu 다중 입력 | `conn-Fy`/`conn-Fu`가 재료와 조용히 어긋남 (편집 가능 + 동기화 없음) | `syncFyValue`/`syncFuValue`에 포함 — 전처리 재료가 단일 소스, 접합부 탭은 힌트 표시 후 수정 가능 |
| Fu≥Fy 검증 위치 | readonly인 design-fu에 붙어 발화 불가 | 편집 가능한 전처리 `input-fu`에서 검증 (`propagateFyFuFromPreprocessor`) |
| DSM 단일옵션 드롭다운 | `select-analysis-method`(옵션 1개) — EWM 시절 잔재 | 고정 라벨 "해석법: DSM (직접강도법, Appendix 2)"로 교체 |
| 버튼 라벨 불일치 | 경고문은 "설계용 FSM 해석 준비", 실제 버튼은 "FSM 결과 준비" | "설계용 FSM 해석 준비"로 통일 (버튼·복원 라벨·힌트) |
| Lap 설계 2탭 왕복 | 하중분석 후 접합부 탭으로 수동 이동해 별도 실행 필요 | 하중분석 결과에 "→ 접합부 탭에서 Lap 접합부 설계 실행" 원클릭 버튼 추가 (자동입력 안내 포함) |

## B4. 알기 어려운 부분 개선

| 항목 | 수정 |
|------|------|
| 부재 유형 선택 시 입력 체계가 몰래 바뀜 | `#member-mode-badge` 신설 — "자동 계산 모드: ①입력→②하중 분석→③설계 검토" / "직접 입력 모드: ..." 상시 표시 |
| K·L(기둥)과 Lb(보)가 "비지지 길이" 한 제목 아래 혼재 | 표시 대상에 따라 동적 제목: "유효좌굴길이 (K·L)" / "비지지길이·모멘트계수 (Lb, Cb)" / 병기 |
| 영/한 라벨 혼용 | 주요 제목 한글화(영문 병기): 단면 입력, 단면 템플릿, 재료, 절점, 요소, 단면 미리보기, DSM 설계값, 좌굴 곡선(Signature Curve), 모드 분류, 소성 상호작용 곡면, 모드 형상(2D/3D), "Generate"→"단면 생성" |
| SI 모드에 "→15 PLF" 하드코딩 | 중립 텍스트로 교체 (updatePLF가 단위계에 맞는 kN/m / PLF 표기) |
| 재료 단일 소스 불명확 | 전처리 재료 힌트에 "해석·설계·접합부 탭 전체 적용(단일 소스)" 명시, 설계 탭은 "읽기전용" 표기 |

---

# Part C. 2차 GUI 재배치 + 접합부 파이프라인 재구축 (2026-07-05 후속)

## C1. 탭 워크플로 재구성 (해석↔설계 중복감 해소)

- 탭 바를 3그룹으로 재구성: **[① 단면·재료 → ② 좌굴해석 → ③ 해석결과] | [④ 부재설계 → ⑤ 접합부] | [보고서 · 검증]** (번호 + 구분선)
- 설계 탭 상단에 **FSM/DSM 연동 상태 스트립**(`#design-dsm-status`) 신설 — 좌굴해석 결과(Pcrl/Pcrd/Mcrl/Mcrd) 연동 시 초록 표시, `dsmResult`/`analysisInvalidated`/`designDsmPrepared`에서 갱신
- Step indicator 한글화, 앱 전체 안내 문구의 탭 명칭을 새 라벨로 통일

## C2. 사용하중/소요강도 정리

- "소요 하중" 블록을 사용하중·"하중 분석 실행" **바로 뒤로 이동** (기존에는 좌굴길이/Cb/β 입력들이 사이에 끼어 있었음)
- 개명: **"사용하중 입력 (미계수 면압, Service Loads)"** / **"소요강도 (계수하중 조합값, Required Strength)"** + 관계 설명 힌트, P/V→Pu/Vu, My→Muy

## C3. 접합부 파이프라인 감사(에이전트) 후 수정 — GUI↔코드 불일치 해소

### 죽은 입력·무시되는 입력 (심각)
| 항목 | 문제 | 수정 |
|------|------|------|
| PAF `Fuf` | 입력을 받고도 무시, 항상 Fuh=120 ksi 하드코딩 | `paf_connection`이 Fuf(>0)를 Fuh로 사용. GUI/MCP/dispatcher 기본값 827 MPa(120 ksi)로 정합화. 테스트(F-018) 새 계약으로 갱신 |
| 스크류 `Fub` | 미전송 → python 기본 100 ksi가 미공개 가정 | Fub 입력행을 스크류에도 표시(기본 689 MPa=100 ksi), 전송 |
| Lap `Mnd`/`Vn` | 항상 미전송 → §F4 뒤틀림·§H2 휨-전단이 경고로만 처리 | 설계 결과(_lastDesignResult)에서 자동 전달 — 8a/8b 스텝이 실제 평가됨 |
| Se 대체 | Se 없으면 총단면 Sx로 조용히 대체(비보수) | 결과 패널에 경고 배너 표시 |

### 규준 조항·라벨 오류
- 필릿 §J2.1→**§J2.5**, 아크시임 §J2.4→**§J2.3**, 그루브 §J2.3→**§J2.1/J2.6** (GUI 옵션·SVG 제목·결과 헤더)
- python arc_seam 식 라벨 J2.2.2.1(아크스폿 번호)→**J2.3.2-1/-2**, spec_sections **J2.3.2**
- 나사 게이지 맵 정정: 0.190 in=**#10** (과거 #14로 오표기; #8=0.164/#12=0.216/#14=0.242 기준 재작성)
- ASD 실행 시에도 "φRn/φMn"으로 표기되던 것 → design_method에 따라 **Rn/Ω·Mn/Ω** 표기

### 유형별 입력 의미 명시 (d·n 의미가 유형마다 다름)
- `#conn-single-hint` 신설: 유형 선택 시 d/n/Fub의 의미와 내부 가정(볼트 표준홀·e=1.5d·s=3d, 필릿 n=용접선 수, PJP te=0.5t 추정 등)을 표시

### SVG 도면 정합화
| 문제 | 수정 |
|------|------|
| 요청 입력이 결과에 없어 도면이 항상 기본값(패스너 1개, t=0.059, PJP)으로 그려짐 | 확장에서 요청 데이터를 결과에 병합(`{...request, ...result}`) — t1/t2/d/n/weld_size/groove_type 실값 반영 |
| `arc_spot_weld`/`groove_weld` 키 불일치로 두 도면이 아예 안 그려짐 | 키 정규화 |
| `r.weld_L` 미존재 필드 참조 | `weld_length`로 수정 |
| Lap 도면이 계산 개수의 2배를 그림 (지점 양측 n개씩) | 열당 n개를 랩 양단에 분할 배치 — 총 표시 = n_rows×n_per_row = 계산값 |
| 부재가 지점 ±0.3ext까지만 그려져 겹침≠랩구간, 패스너가 한 부재만 관통 | 부재를 랩 경계까지 연장 |
| 단부거리 e를 지점 중심 기준으로 표기 | 랩 끝 기준(§J3.2/J4.2 end distance)으로 수정 |
| n=0이어도 `||2` fallback으로 2개 그림 | nullish 체크로 0을 정직하게 표시 |
| 필릿 크기 라벨 스케일 오류(항상 최소) | US in 기준 스케일 수정 |

### 결과 렌더러
- Lap 오류 응답 시 기본값 도면+NG 대신 **오류 메시지 표시**
- 헤드라인 휨 DCR이 국부(§F3)만 반영해 8a/8b 실패와 모순 가능 → python이 `flexure_dcr_governing`(F3/F4/H2 최대) 반환, 렌더러/보고서/검증 모두 이 값 사용
- **패스너 한계상태 표(1개당)** 신설 — 어떤 모드(풀아웃/베어링/전단 등)가 Pns를 지배했는지 공개 (기존에는 계산만 되고 미표시)
- §J6.1 인열 지배 시 spacing/판정 재산정 (Step 5·반환값·도면 일관성)
- SI 모드에서 US 단위 수식 문자열에 대한 안내 문구 추가
- "필요 패스너 수(행 반올림)", 간격 N/A(열당 1개) 등 라벨 정밀화

# 검증

- `npm run compile` (tsc) 통과
- `node --check` app.js / designState.js 통과
- Python 단위테스트 **63/63 통과** (PAF F-018 테스트는 새 Fuf 계약으로 갱신)
- Part A/C는 스모크 테스트로 수치 검증 (ASD Rn/Ω vs LRFD φRn 분리, 교차구간 지배식, alpha_y>1 발동, Mn=0→pass=False, PAF Fuf 반영/기본 120, arc seam J2.3.2 라벨, 게이지 맵, Lap 8a/8b 스텝 생성, §J6.1 재산정 spacing 일관성)

## 수정 파일 목록

| 파일 | 주요 변경 |
|------|-----------|
| `python/design/aisi_s100.py` | ry 별칭·fallback, 휨 Mn≤0 가드 |
| `python/design/special_topics.py` | block_shear ASD/LRFD·connection_type |
| `python/design/connections.py` | 그루브 용접 지배식 design_method 분기 |
| `python/design/global_buckling.py` | `_last_detail` 진입 초기화 |
| `src/mcp/server.ts` | check_block_shear 스키마 확장 |
| `src/webview/StcfsdPanel.ts` | 라벨 정리, 모드 배지, 분류 버튼, DSM 라벨, `_classifyModes` 페이로드 |
| `webview/js/app.js` | 표 편집 반영, 리스너 누적·race 수정, Fy/Fu 동기화 정리, 분류 버튼 배선, Lap 원클릭, 죽은 코드 제거 |
| `webview/js/designState.js` | no-op 저장/복원 제거 |
| `webview/css/theme.css` | CSP 차단 폰트 @import 제거 |

---

# Part D. 접합부 설계과정 재검토 + 탭 통합 재편 (2026-07-05 3차)

## D1. Lap 접합부 설계과정 결함 수정 (사용자 보고: Vn 미입력·Se 대체·볼트 과다)

| 원인 | 수정 |
|------|------|
| 휨 설계 결과에 `Vn`이 없어(조합 경로에서만 `shear.Vn` 생성) §H2 검토가 영구 미실행 | **lap_connection이 §G2.1(비보강 웹)로 Vn을 직접 산정** (d·t·Fy 보유) — Step '8p'로 산정 과정 공개, 입력 시 입력값 우선 |
| 휨 설계 결과에 `Se` 키가 없어 설계를 돌려도 항상 "Sx 대체" 경고 | 클라이언트가 **Se = Mnl/Fy_used** (DSM 국부좌굴 반영 등가 유효단면계수)로 도출해 전달 |
| 전단 수요인데 **인장계 한계상태(풀아웃 J4.4.1 ≈ 전단모드의 1/2.7)가 Pns를 지배** → 스크류 개수 과대 | Pns를 **전단 전달 한계상태만**(틸팅/지압/나사전단/볼트전단/J6.1)으로 산정. `Pns_governing_mode` 반환, 한계상태 표에 ★·제외 사유 명시. 테스트 2건 새 계약으로 갱신 |
| 볼트 단부거리를 최소 1.5d로 고정 가정 → §J6.1 인열이 항상 지배해 개수 폭증 | **단부거리 e 입력 신설**(`conn-edge-dist`, 0=자동 1.5d) — e 증가 시 인열강도 비례 증가. 경고문에 완화 방법 안내 |

## D2. 탭 통합 재편 (사용자 제안 반영)

**새 구성: ① 단면·재료 | ② 하중계산 | ③ 좌굴해석·부재설계 | ④ 접합부 | 보고서 · 검증**

- **② 하중계산 (신설)**: 부재설계에 있던 하중계산 과정 전체 이동 — 부재 구성(스팬/지점/랩), 사용하중, 데크&가새, "하중 분석 실행" + 하중 분석 결과 패널. 완료 시 "→ ③ 설계 검토 실행" / "→ ④ Lap 접합부 설계" 원클릭 이동 버튼 제공. 직접 입력 모드일 때는 안내 문구 표시
- **③ 좌굴해석·부재설계 (통합)**: 구 ②좌굴해석 탭(하중케이스/경계조건/반파장/cFSM/해석 실행/응력 프리뷰)을 **"A. 좌굴해석"** 접이식 섹션으로, 구 ③해석결과 탭(DSM 설계값/좌굴곡선/모드분류/소성곡면/모드형상 2D·3D)을 **"B. 좌굴해석 결과"** 섹션으로, 기존 설계 입력/결과를 **"C. 부재 설계"** 섹션으로 통합. 해석 완료 시 B 섹션 자동 펼침 + 통합 탭으로 전환
- 좌굴해석 이중 실행 문제: FSM 해석(A)과 설계용 FSM 준비(C)가 한 화면에 있어 관계가 시각적으로 명확해짐 (FSM 연동 상태 스트립이 상단에서 안내)
- 모든 요소 id 보존 → 저장/복원(designState)·트리뷰·보고서·검증 로직 무변경. tabMap/focusMap 재매핑, 탭 번호 참조 문구 전면 갱신, CSS 스코프(#tab-loads) 확장

# 최종 검증 (Part D 포함)

- `npm run compile` 통과, `node --check` 통과
- Python 단위테스트 **63/63 통과** (lap 전단모드 계약 테스트 갱신 포함)
- 스모크: 스크류 Pns 0.28→0.47 kips(풀아웃 제외), Vn 자동산정 3.71 kips + 8b(H2) 스텝 생성, 볼트 e 확대 시 Pns 증가 확인
