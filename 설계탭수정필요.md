# 설계탭 수정 필요 사항

작성일: 2026-04-14
수정일: 2026-04-14 (이슈 1~3 수정 완료)
추가 수정일: 2026-04-14 (이슈 4~5 수정 완료)

## 이슈 1: 모멘트도/전단력도/처짐도에서 랩 효과 미반영

### 1-1. M/V 다이어그램 (모멘트, 전단)

**현상**: 자유단-핀단-핀단-자유단(N-P-P-N) 구조물에서 핀단에 설정한 랩의 효과가 다이어그램에 반영되지 않는 것처럼 보임

**분석 결과**: M/V 계산 자체는 FE 해석(`analyze_beam_fe()`)으로 랩 효과(비등단면 EI)를 반영하고 있음. 물리적으로 모멘트 다이어그램은 랩 경계에서 꺾이지 않음 -- 등분포하중에서 포물선 형태를 유지하며 랩의 효과는 **값의 재분배**(지점 부모멘트 증가, 스팬 정모멘트 감소)로만 나타남.

### 1-2. 처짐 다이어그램 -- 핵심 문제

**현상**: 랩 구간과 비랩 구간의 경계에서 처짐 곡선에 변화(곡률 변화)가 있어야 하지만 부드러운 곡선으로만 그려짐

#### 문제 A: 처짐이 FE 결과를 직접 사용하지 않고 재계산

- **위치**: `python/design/loads/required_strength.py:312-319`
- **내용**: 개별 하중 케이스의 `analyze_beam_fe()` 결과에 D(처짐)가 이미 포함되어 있으나, `apply_combination()`에서 M/V/R만 조합하고 **D는 조합하지 않음** (`load_combinations.py:81-131`). 대신 `compute_deflection_variable_I()`로 새 FE 모델을 만들어 재계산함.
- **해결**: `apply_combination()`에서 D도 선형 조합하여 직접 사용

#### 문제 B: 요소 강성 산정 불일치

- **위치**: `python/design/loads/beam_analysis.py`
- **내용**: `analyze_beam_fe()` (line 960)은 `max(I[i], I[i+1])`을 사용하고, `compute_deflection_variable_I()` (line 798)은 `(I[i] + I[i+1]) / 2`를 사용. 랩 경계에서 25% 강성 차이 발생.
- **해결**: `compute_deflection_variable_I()`도 `max()` 방식으로 통일

#### 수정 방향
- `load_combinations.py`의 `apply_combination()`에서 D 배열도 선형 조합
- `required_strength.py`에서 FE D 결과가 있으면 직접 사용, fallback으로만 `compute_deflection_variable_I()` 호출
- `compute_deflection_variable_I()`의 EI 산정을 `max()` 방식으로 변경

---

## 이슈 2: 접합부 설계에 잘못된 모멘트값 적용

### 2-A. _autoFillLapInputs에서 잘못된 모멘트 선택

- **위치**: `webview/js/app.js:4044-4053`
- **현상**: 그래프에서 핀단 부모멘트가 -1.7 kip-ft로 나타나지만 접합부 설계 입력(`conn-Mu`)에 다른 값이 들어감
- **원인**: `_autoFillLapInputs()`에서 "Lap end"만 제외하고 **모든 위치(정모멘트 포함)에서 최대 |Mu|**를 선택. 스팬 중앙의 정모멘트가 핀 부모멘트보다 크면 정모멘트가 입력됨.
- **해결**: `Mu < 0`인 지점(부모멘트) 위치만 필터링하여 `conn-Mu` 자동입력

### 2-B. 단위 불일치 (kip-ft vs kip-in) -- Critical

- **위치**: `src/webview/StcfsdPanel.ts:1479-1480`
- **현상**: Lap 구간 이용률(utilization)이 12배 작게 계산됨
- **원인**: `negMuSupport`는 kip-ft 단위(locations에서 가져옴), `lapPhiMn`은 kip-in 단위(aisi_design에서 Se x Fy). 단위 변환 없이 직접 나눗셈.
- **예시**: `1.7 kip-ft / 20 kip-in = 0.085` (실제: `1.7 x 12 / 20 = 1.02`)
- **해결**: `negMuSupport * 12`로 kip-in 변환 후 비교

---

## 이슈 3: 접합부 설계 도면 품질 개선

- **위치**: `webview/js/app.js:962-1102` (`_drawLapSvg()`)
- **현상**: 랩 접합부 그림이 명확하지 않음. 부재 형상, 스크류/볼트 위치, 치수 등이 도면 수준으로 표현되어야 함.

### 문제점

| 항목 | 현재 상태 | 필요 사항 |
|------|-----------|-----------|
| 단면 형상 | 고정 픽셀값 (webH=48, flangeW=14 등) | 실제 d, b, c, t에 비례하는 스케일 |
| 부재 표현 | 측면도(side view)만 있음 | 측면도 + 단면도(cross-section) 추가 |
| 패스너 위치 | 웹 중간에 균등 배치 | 플랜지/웹 접합부 기준 정확한 위치 |
| 치수선 | Lap 길이, 간격, 연단거리만 | 부재 높이 d, 플랜지폭 b, 립 c, 두께 t 추가 |
| 겹침 표현 | 반투명 사각형 음영만 | 2겹 겹침부의 실제 단면 형상 상세 |
| 스크류/볼트 | 원+X표시 (고정 3.5px) | 직경 비례 크기, 머리/몸체 구분 |
| 단면도 | 없음 | 랩 구간의 2겹 단면 절단도(Cut A-A) 추가 |

---

## 수정 우선순위 및 완료 현황

| # | 문제 | 심각도 | 파일 | 상태 | 수정 내용 |
|---|------|--------|------|------|-----------|
| 2B | 단위 불일치 (kip-ft vs kip-in) | Critical | StcfsdPanel.ts:1468-1484 | **완료** | `negMuSupport * 12`로 kip-in 변환 후 utilization/pass 계산 |
| 2A | conn-Mu 자동입력 시 정/부모멘트 미구분 | High | app.js:4041-4059 | **완료** | `loc.Mu < 0` 필터 추가 — 부모멘트(지점)만 선택 |
| 1A | apply_combination에서 D 조합 누락 | Medium | load_combinations.py:81-132 | **완료** | D 배열 선형 조합 로직 추가, 반환값에 'D' 키 포함 |
| 1B | 처짐 EI 산정 불일치 (avg vs max) | Medium | beam_analysis.py:798 | **완료** | `(I[i]+I[i+1])/2` → `max(I[i], I[i+1])`로 변경 (analyze_beam_fe와 일치) |
| 1C | required_strength.py에서 FE D 직접 사용 | Medium | required_strength.py:290-336 | **완료** | FE D 조합 결과 우선 사용, 없으면 fallback으로 compute_deflection_variable_I 호출 |
| 3 | 접합부 도면 품질 개선 | Medium | app.js:962-1102 | **완료** | 전면 재작성 (아래 상세) |

---

## 수정 상세

### 2B. StcfsdPanel.ts 단위 수정

**변경 전:**
```typescript
utilization: negMuSupport / (dm === 'LRFD' ? lapPhiMn : lapMnOmega)
pass: negMuSupport <= (dm === 'LRFD' ? lapPhiMn : lapMnOmega)
```

**변경 후:**
```typescript
const negMuSupport_kipin = negMuSupport * 12;  // kip-ft → kip-in
utilization: negMuSupport_kipin / (dm === 'LRFD' ? lapPhiMn : lapMnOmega)
pass: negMuSupport_kipin <= (dm === 'LRFD' ? lapPhiMn : lapMnOmega)
```

### 2A. app.js 부모멘트 필터링

**변경 전:** 모든 위치에서 최대 |Mu| 선택
```javascript
if (loc.Mu != null && Math.abs(loc.Mu) > Math.abs(maxMu)) maxMu = loc.Mu;
```

**변경 후:** 부모멘트(Mu < 0)인 지점만 선택
```javascript
if (loc.Mu != null && loc.Mu < 0 && Math.abs(loc.Mu) > Math.abs(maxNegMu)) maxNegMu = loc.Mu;
```

### 1A. load_combinations.py D 조합 추가

`apply_combination()`에서 D(처짐) 배열도 M/V/R과 함께 선형 조합하여 반환값에 `'D'` 키 포함.

### 1B. beam_analysis.py EI 산정 통일

`compute_deflection_variable_I()` 내부 FE 조립 시:
- 변경 전: `EI_e = E * (I_at_x[i] + I_at_x[i+1]) / 2.0` (평균)
- 변경 후: `EI_e = E * max(I_at_x[i], I_at_x[i+1])` (최대값, analyze_beam_fe와 일치)

### 1C. required_strength.py FE D 직접 사용

하중조합된 D 배열(`svc_D_combined`)이 유효하면 직접 사용. FE D가 없거나 모두 0이면 기존 `compute_deflection_variable_I()` fallback 경로 유지.

### 3. _drawLapSvg 전면 개선

`_drawLapSvg()` 함수를 전면 재작성:

1. **실제 치수 비례 스케일링**: 고정 픽셀 대신 실제 d, b, c, t 값 기반 비례 렌더링
   - `scH`, `scV` 스케일 팩터로 인치→픽셀 변환
   - UI 입력(tpl-B, tpl-C)에서 플랜지폭, 립 높이 동적 읽기
2. **단면도(Cut A-A) 추가**: SVG 우측 35% 영역에 2겹 C단면 절단도
   - M1/M2 두 부재의 nested 겹침 형상
   - 패스너 위치 표시
   - d, b, c, t 치수선
3. **측면도 개선**:
   - Lap 경계 세로 점선 표시
   - Cut A-A 절단선(분홍색 일점쇄선) 표시
   - 부재 높이 d 세로 치수선
   - 패스너 크기가 실제 직경에 비례
   - 스크류/볼트 구분 표시
4. **Python 결과에 d, t, lap 치수 포함**: `lap_connection.py` 반환값에 `d`, `t`, `lap_left_in`, `lap_right_in` 추가

---

## 이슈 4: 풍하중 정압/부압 미분리

### 현상
풍하중 입력이 Wu(양력/부압) 하나만 존재. 풍정압(Wp, 하향)은 입력 불가.

### 원인
- `StcfsdPanel.ts:2332-2335`: Wu 입력만 존재, Wp 입력 없음
- `app.js:3510`: `W: -(loadWu_psf * spacingUS)` — 항상 음수로 변환하여 양력으로만 작용

### 수정 내용

| 파일 | 변경 |
|------|------|
| `StcfsdPanel.ts:2336-2340` | Wp 입력 필드 추가 (`id="load-Wp-psf"`, 라벨 "Wp↓") |
| `app.js:90` | SI 기본값에 `'load-Wp-psf': [0, 'pressure']` 추가 |
| `app.js:162` | 단위 변환 리프레시에 `'load-Wp-psf'` 추가 |
| `app.js:3083` | PLF 자동 변환에 `'Wp'` 추가 |
| `app.js:3092` | 입력 이벤트에 `'load-Wp-psf'` 추가 |
| `app.js:3497-3512` | loads에 `Wp: loadWp_psf * spacingUS` (양수, 하향) 추가 |
| `required_strength.py:74-115` | Wp 별도 해석 후, 'W' 슬롯에 Wp 결과 대입하여 하중조합 2차 실행 → 지배조합 병합 |
| `load_combinations.py` | 변경 없음 — Wp는 기존 'W' 하중계수를 양수로 재사용 |
| `designState.js` | `loadWp` 수집/복원 추가 |

### 동작 원리
- Wu(부압): `W = -(Wu × spacing)` → 음수 → `0.9D+1.0W`에서 양력 발생
- Wp(정압): `Wp = +(Wp × spacing)` → 양수 → `1.2D+1.0W`에서 중력 하중 증가
- 하중조합 엔진은 Wu/Wp 각각에 대해 독립적으로 실행 후 지배조합 병합

---

## 이슈 5: 양력 지배 시 Lb/Cb 계산 오류

### 현상
양력 하중조합이 지배할 때, 중력 M_diagram 기준의 Lb/Cb를 사용하여 설계. 압축 플랜지 위치가 반전됨에도 불구하고 동일한 비지지길이 적용.

### 원인
- `required_strength.py:174-282`: `auto_params` (Lb, Cb)를 gravity_result M_diagram만으로 계산
- 양력 시 압축 플랜지가 반전:
  - 중력: 정모멘트=상부압축(데크지지), 부모멘트=하부압축(비지지)
  - 양력: 정모멘트=하부압축(비지지!), 부모멘트=상부압축(데크지지)
- 양력에 대한 별도 Lb/Cb 계산 없음

### 수정 내용

| 파일 | 변경 |
|------|------|
| `required_strength.py:284-340` | 양력 M_diagram 부호 반전(`-M`)하여 `determine_unbraced_lengths()` 재호출. 반전된 M에서 정모멘트=원래 부모멘트(데크지지), 부모멘트=원래 정모멘트(비지지). 결과를 `auto_params['uplift_bracing']`에 저장 |
| `StcfsdPanel.ts:1490-1518` | `design_uplift` 추가: `uplift_bracing.negative_region`의 Lb/Cb와 양력 최대 |Mu|로 AISI 설계 수행. `result`에 `design_uplift` 포함 |

### 부호 반전 로직 설명

```
양력 M_diagram: [-2.5, -1.0, 0.5, 1.2, 0.5, -1.0, -2.5]
                 (경간 중앙이 음수 → 하부 압축 → 비지지)

부호 반전 (-M): [2.5, 1.0, -0.5, -1.2, -0.5, 1.0, 2.5]
                 (반전 후: 중앙이 부모멘트 → determine_unbraced_lengths가
                  이 구간의 Lb를 "부모멘트 비지지"로 계산)
```

반전 후에는 중력과 동일한 `determine_unbraced_lengths` 로직이 그대로 적용됨:
- 반전 후 정모멘트 = 원래 부모멘트 구간 → 데크 지지 (상부 압축)
- 반전 후 부모멘트 = 원래 정모멘트 구간 → 비지지 (하부 압축)

---

## 수정 우선순위 및 완료 현황 (업데이트)

| # | 문제 | 심각도 | 파일 | 상태 |
|---|------|--------|------|------|
| 2B | 단위 불일치 (kip-ft vs kip-in) | Critical | StcfsdPanel.ts | **완료** |
| 6 | 정/부모멘트 설계 Mu 단위 미변환 (kip-ft→kip-in) | Critical | StcfsdPanel.ts:1418,1435 | **완료** |
| 7 | 1.2D+1.6L 독립 LRFD 조합 누락 | High | load_combinations.py | **완료** |
| 2A | conn-Mu 정/부모멘트 미구분 | High | app.js | **완료** |
| 5 | 양력 지배 시 Lb/Cb 오계산 | High | required_strength.py, StcfsdPanel.ts | **완료** |
| 4 | 풍하중 정압/부압 미분리 | Medium | StcfsdPanel.ts, app.js, required_strength.py, designState.js | **완료** |
| 1A | apply_combination D 조합 누락 | Medium | load_combinations.py | **완료** |
| 1B | 처짐 EI 산정 불일치 | Medium | beam_analysis.py | **완료** |
| 1C | FE D 직접 사용 | Medium | required_strength.py | **완료** |
| 3 | 접합부 도면 품질 개선 | Medium | app.js | **완료** |

---

## 이슈 6: 정/부모멘트 설계 Mu 단위 미변환 (전체 코드 감사에서 발견)

### 현상
퍼린 설계 시 정모멘트/부모멘트의 이용률(utilization)이 실제보다 **12배 과소평가**됨. 실패해야 할 설계가 통과.

### 원인
- `StcfsdPanel.ts:1418`: `Mu: posMu` — kip-ft 그대로 전달
- `StcfsdPanel.ts:1435`: `Mu: negMu` — kip-ft 그대로 전달
- `aisi_design()`의 Mn, φMn은 kip-in 단위 → 비교 시 12배 차이
- Lap 설계(line 1470)에서만 `×12` 변환이 되어 있었음

### 수정
- `Mu: posMu * 12` (line 1418)
- `Mu: negMu * 12` (line 1435)
- `Mu: upliftMu * 12` (line 1506, 양력 설계)

## 이슈 7: 1.2D+1.6L 독립 LRFD 조합 누락

### 현상
D와 L만 존재하고 Lr/S가 없는 경우(바닥 장선), LRFD 조합이 `1.4D`밖에 적용되지 않음.

### 원인
`load_combinations.py`에 `1.2D+1.6L` 독립 조합이 없어서, 필터링 시 Lr/S가 미입력이면 `1.2D+1.6L+0.5Lr` 등이 모두 제외됨.

### 수정
`LRFD_COMBOS`에 `('2: 1.2D+1.6L', {'D': 1.2, 'L': 1.6})` 추가

---

## 전체 코드 감사 결과 (2026-04-14)

4개 영역 병렬 감사(단위변환, 하중조합, AISI 설계, 가새/Cb) 수행.

### 검증 통과 항목

| 영역 | 파일 | 상태 | 비고 |
|------|------|------|------|
| DSM 압축 국부좌굴 (§E3.2.1) | dsm_strength.py | ✅ 정확 | λl=0.776, 지수 0.4 |
| DSM 압축 뒤틀림좌굴 (§E4.1) | dsm_strength.py | ✅ 정확 | λd=0.561, 지수 0.6 |
| DSM 휨 국부좌굴 (§F3.2.1) | dsm_strength.py | ✅ 정확 | λl=0.776, 지수 0.4 |
| DSM 휨 뒤틀림좌굴 (§F4.1) | dsm_strength.py | ✅ 정확 | λd=0.673, 지수 0.5 |
| 전체좌굴 압축 (§E2) | global_buckling.py | ✅ 정확 | λc=1.5, 0.658/0.877 |
| 전체좌굴 LTB (§F2) | global_buckling.py | ✅ 정확 | Fcre, 2.78Fy, 0.56Fy |
| LTB Fcre (§F2.1) | global_buckling.py | ✅ 정확 | C/Z 구분, σey, σt |
| 비탄성예비 (§F2.4.2) | global_buckling.py | ✅ 정확 | Mcre>2.78My, Mp 한계 |
| 냉간가공 (§A3.3.2) | special_topics.py | ✅ 정확 | Fya, Bc, m, Fu/Fy≥1.2 |
| 웹크리플링 (§G5) | shear.py | ✅ 정확 | G5-1~5 계수테이블, α 공식 |
| φ/Ω 계수 (§B3.2) | aisi_s100.py | ✅ 정확 | φb=0.90, Ωb=1.67 등 |
| DSM 적용한계 (Table B4.1-1) | aisi_s100.py | ✅ 정확 | w/t≤500, b/t≤160 등 |
| Cb 공식 (F2.1.1-2) | bracing.py | ✅ 정확 | 12.5Mmax/(2.5+3+4+3), ≥1.0 |
| kφ 회전강성 | bracing.py | ✅ 정확 | Chen & Moen 2011 직렬스프링 |
| PLF→kip/ft 변환 | beam_analysis.py | ✅ 정확 | /1000 |
| 처짐 단위 | beam_analysis.py | ✅ 정확 | inches |
| Fy/Fu 단위 | steel_grades.py | ✅ 정확 | ksi |

### 참고 사항 (설계 관행상 허용, 주의 필요)

| 항목 | 위치 | 설명 |
|------|------|------|
| 변곡점 가새점 취급 | bracing.py:331-385 | AISC 엄밀 해석에서는 비허용이나, CFS 퍼린 설계 실무에서 널리 사용되는 근사법. AISI S100 commentary 참조 |
| ASCE 7-10 기준 | load_combinations.py:4 | 하중조합이 ASCE 7-10 기준. 7-22와 계수 차이 미미하나 기준 명시 필요 |
| Z단면 Fcre /2 | global_buckling.py:241 | 점대칭 단면 S100-16 §F2.1.3 적용. 표준 Z단면에 적합, 비표준 형상 시 확인 필요 |
| 웹크리플링 G5-1 미구현 행 | shear.py:263-266 | Built-up I단면 unstiffened two-flange 미구현 (명시적 에러 반환) |
| Lb=0 시 Fcre=1e6 | global_buckling.py:226-227 | 완전 횡지지 근사. 수치적 안정성 확보용 |
